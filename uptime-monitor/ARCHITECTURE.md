# 📐 Architecture Diagrams

Visual architecture documentation untuk Uptime Monitor.

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         End Users / Clients                          │
│                    (Web Browser / Mobile App)                        │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ HTTPS
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                       Reverse Proxy / CDN                            │
│                    Nginx / CloudFlare                                │
│                  (SSL Termination, Load Balancing)                   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
┌─────────────▼────────────┐   ┌───────────▼──────────────┐
│   Frontend (SPA)         │   │   Backend API            │
│   Vue.js 3 + Vite        │   │   Laravel 11             │
│   Port: 5173 (dev)       │   │   Port: 8000 (dev)       │
│   - Reactive UI          │   │   - RESTful API          │
│   - Pinia State Mgmt     │   │   - JWT Auth             │
│   - Vue Router           │   │   - Queue System         │
│   - Axios HTTP           │   │   - Scheduler            │
└──────────────────────────┘   └───────────┬──────────────┘
                                           │
                                           │
                   ┌───────────────────────┼───────────────────────┐
                   │                       │                       │
         ┌─────────▼─────────┐  ┌─────────▼─────────┐  ┌─────────▼─────────┐
         │  Queue Workers     │  │  Scheduler (Cron) │  │   Database        │
         │  Supervisor        │  │  Laravel Schedule │  │   PostgreSQL      │
         ├────────────────────┤  ├───────────────────┤  ├───────────────────┤
         │ Monitor Checks     │  │ - Monitor Checks  │  │ - Monitors        │
         │ (2 processes)      │  │ - Metrics Agg.    │  │ - Incidents       │
         │                    │  │ - Data Cleanup    │  │ - Checks          │
         │ Notifications      │  │ - Log Rotation    │  │ - Notifications   │
         │ (1 process)        │  │                   │  │ - Users           │
         └─────────┬──────────┘  └───────────────────┘  └───────────────────┘
                   │
                   │ Send Notifications
                   │
         ┌─────────▼──────────────────────────────────┐
         │        External Services                    │
         ├─────────────────────────────────────────────┤
         │  - Discord Webhook API                      │
         │  - Telegram Bot API                         │
         │  - Slack Webhook API                        │
         │  - Generic Webhooks                         │
         │  - SMS (Twilio) - Future                    │
         │  - Email (SMTP) - Future                    │
         └─────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Monitor Creation Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Fill form (name, type, target, interval)
     │
┌────▼─────────────┐
│  CreateMonitor   │
│  View (Vue)      │
└────┬─────────────┘
     │ 2. POST /api/monitors
     │    { name, type, target, interval_seconds, is_public, ... }
     │
┌────▼──────────────────┐
│  MonitorController    │
│  @store               │
├───────────────────────┤
│ 3. Validate input     │
│ 4. Create in DB       │
│ 5. Dispatch job       │
└────┬──────────────────┘
     │
     ├─────────────────┬─────────────────────┐
     │                 │                     │
┌────▼────────┐  ┌────▼────────────┐  ┌────▼─────────────┐
│  Database   │  │ ProcessMonitor  │  │  HTTP Response   │
│             │  │ Check Job       │  │  201 Created     │
│ INSERT INTO │  │ (Immediate)     │  │  { success, data }│
│ monitors    │  │                 │  └──────────────────┘
└─────────────┘  └────┬────────────┘
                      │ 6. Execute first check
                      │
                 ┌────▼────────────┐
                 │ HTTP/PING/PORT  │
                 │ Health Check    │
                 └────┬────────────┘
                      │ 7. Save result
                      │
                 ┌────▼────────────┐
                 │  Database       │
                 │  INSERT INTO    │
                 │  monitor_checks │
                 │  UPDATE monitors│
                 └─────────────────┘
```

### 2. Monitoring Check Flow

```
┌──────────────────┐
│  Cron (Every 1s) │
└────────┬─────────┘
         │
         │ * * * * * php artisan schedule:run
         │
┌────────▼──────────────┐
│  Laravel Scheduler    │
│  (routes/console.php) │
└────────┬──────────────┘
         │
         │ Schedule::command('monitor:check')->everySecond()
         │
┌────────▼───────────────────┐
│  RunMonitorChecks Command  │
│  (Custom Worker)           │
├────────────────────────────┤
│ 1. Find monitors due       │
│    - enabled = true        │
│    - next_check_at <= now  │
└────────┬───────────────────┘
         │
         │ For each monitor...
         │
┌────────▼────────────────────┐
│  ProcessMonitorCheck Job    │
│  (Queue: monitor-checks)    │
├─────────────────────────────┤
│ 2. Execute health check     │
│    match (type):            │
│      'http' => checkHttp()  │
│      'ping' => checkPing()  │
│      'port' => checkPort()  │
└────────┬────────────────────┘
         │
         ├─────────────┬──────────────┬────────────────┐
         │             │              │                │
    ┌────▼────┐  ┌────▼─────┐  ┌────▼──────┐  ┌─────▼────────┐
    │ HTTP    │  │  PING    │  │  PORT     │  │  SSL Check   │
    │ Request │  │  ICMP    │  │  TCP      │  │  Certificate │
    │ Guzzle  │  │  Exec    │  │  Socket   │  │  OpenSSL     │
    └────┬────┘  └────┬─────┘  └────┬──────┘  └─────┬────────┘
         │             │              │                │
         └─────────────┴──────────────┴────────────────┘
                              │
                              │ 3. Process result
                              │
                    ┌─────────▼─────────┐
                    │  Status: UP/DOWN  │
                    │  Response Time    │
                    │  Error Message    │
                    └─────────┬─────────┘
                              │
                ┌─────────────┼─────────────┐
                │                           │
         ┌──────▼────────┐           ┌─────▼──────┐
         │  Save Check   │           │ Service UP │
         │  Result       │           │ (Normal)   │
         │               │           └────────────┘
         │ INSERT INTO   │
         │ monitor_checks│
         └───────────────┘
                │
                │ 4. Check status change
                │
         ┌──────▼────────┐
         │ Previous: UP  │
         │ Current: DOWN │
         └──────┬────────┘
                │
                │ Status changed!
                │
         ┌──────▼─────────────┐
         │  Create/Update     │
         │  Incident          │
         │                    │
         │ INSERT INTO        │
         │ incidents          │
         │ (status = 'open')  │
         └──────┬─────────────┘
                │
                │ 5. Dispatch notification
                │
         ┌──────▼─────────────────┐
         │  SendNotification Job  │
         │  (Queue: notifications)│
         └────────────────────────┘
```

### 3. Notification Flow

```
┌────────────────────────┐
│  Incident Created      │
│  (Service Down)        │
└───────────┬────────────┘
            │
            │ Dispatch SendNotification::dispatch($incident)
            │
┌───────────▼─────────────────┐
│  SendNotification Job       │
│  (Queue: notifications)     │
├─────────────────────────────┤
│ 1. Get notification channels│
│    from monitor config      │
└───────────┬─────────────────┘
            │
            │ For each channel...
            │
┌───────────▼──────────────────┐
│  Notification Channels       │
│                              │
│  - Discord (enabled)         │
│  - Telegram (enabled)        │
│  - Slack (disabled)          │
└───────────┬──────────────────┘
            │
            │ Send to enabled channels
            │
     ┌──────┴──────┬──────────┐
     │             │          │
┌────▼─────┐ ┌────▼──────┐ ┌─▼────────┐
│ Discord  │ │ Telegram  │ │  Slack   │
│ Webhook  │ │  Bot API  │ │ Webhook  │
├──────────┤ ├───────────┤ ├──────────┤
│ POST     │ │ POST      │ │ POST     │
│ webhook  │ │ sendMsg   │ │ webhook  │
│ URL      │ │ API       │ │ URL      │
└────┬─────┘ └────┬──────┘ └─┬────────┘
     │            │           │
     │ HTTP 200   │ HTTP 200  │ SKIP (disabled)
     │            │           │
     └────────────┴───────────┘
                  │
                  │ Update last_notification_sent
                  │
         ┌────────▼──────────┐
         │  Database         │
         │  UPDATE monitors  │
         │  SET              │
         │  last_notif_sent  │
         └───────────────────┘
```

### 4. Incident Resolution Flow

```
┌─────────────┐
│  Admin/User │
└──────┬──────┘
       │ 1. View incident details
       │
┌──────▼───────────┐
│ IncidentDetail   │
│ View (Vue)       │
└──────┬───────────┘
       │ 2. Click "Acknowledge" or "Resolve"
       │
┌──────▼────────────────────┐
│ POST /api/incidents/{id}/ │
│ acknowledge OR resolve    │
│                           │
│ { resolution_notes: ... } │
└──────┬────────────────────┘
       │
┌──────▼──────────────────┐
│ IncidentController      │
│ @acknowledge / @resolve │
├─────────────────────────┤
│ 3. Validate input       │
│ 4. Update incident      │
│ 5. Send notification    │
└──────┬──────────────────┘
       │
       ├──────────────┬────────────────┐
       │              │                │
┌──────▼─────┐  ┌────▼───────┐  ┌────▼─────────────┐
│ Database   │  │ SendNotif  │  │ HTTP Response    │
│            │  │ Job        │  │ 200 OK           │
│ UPDATE     │  │            │  │ { success, data }│
│ incidents  │  │ "Resolved" │  └──────────────────┘
│ SET status │  │  message   │
│ = resolved │  └────────────┘
└────────────┘
```

---

## 🗂️ Database Schema Diagram

```
┌─────────────────────────────────────┐
│            users                     │
├─────────────────────────────────────┤
│ id (PK)                             │
│ name                                │
│ email (unique)                      │
│ password                            │
│ role (admin/user)                   │
│ created_at                          │
│ updated_at                          │
└──────────────┬──────────────────────┘
               │
               │ created_by (FK)
               │
┌──────────────▼──────────────────────┐
│          monitors                    │
├─────────────────────────────────────┤
│ id (PK)                             │
│ name                                │
│ type (http/ping/port/ssl)           │
│ target (URL/IP)                     │
│ group_name                          │
│ interval_seconds                    │
│ timeout_ms                          │
│ retries                             │
│ enabled                             │
│ is_public                           │
│ last_status (up/down/unknown)       │
│ last_checked_at                     │
│ next_check_at                       │
│ created_by (FK → users.id)          │
│ created_at                          │
│ updated_at                          │
└──────────────┬──────────────────────┘
               │
          ┌────┴────┬──────────────────┐
          │         │                  │
          │         │                  │
┌─────────▼───────┐ │    ┌─────────────▼─────────┐
│  monitor_checks │ │    │  incidents             │
├─────────────────┤ │    ├───────────────────────┤
│ id (PK)         │ │    │ id (PK)               │
│ monitor_id (FK) │ │    │ monitor_id (FK)       │
│ status          │ │    │ status (open/ack/res) │
│ response_time   │ │    │ severity              │
│ status_code     │ │    │ started_at            │
│ error_message   │ │    │ acknowledged_at       │
│ checked_at      │ │    │ resolved_at           │
│ created_at      │ │    │ description           │
│ updated_at      │ │    │ resolution_notes      │
└─────────────────┘ │    │ created_at            │
                    │    │ updated_at            │
                    │    └───────────────────────┘
                    │
          ┌─────────▼────────────────────┐
          │ monitor_metric_aggregated    │
          ├──────────────────────────────┤
          │ id (PK)                      │
          │ monitor_id (FK)              │
          │ period (minute/hour/day)     │
          │ period_start                 │
          │ total_checks                 │
          │ successful_checks            │
          │ failed_checks                │
          │ avg_response_time            │
          │ min_response_time            │
          │ max_response_time            │
          │ uptime_percentage            │
          │ created_at                   │
          │ updated_at                   │
          └──────────────────────────────┘

┌─────────────────────────────────────┐
│     notification_channels            │
├─────────────────────────────────────┤
│ id (PK)                             │
│ name                                │
│ type (discord/telegram/slack)       │
│ config (JSON)                       │
│   - webhook_url                     │
│   - bot_token                       │
│   - chat_id                         │
│ enabled                             │
│ created_by (FK → users.id)          │
│ created_at                          │
│ updated_at                          │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│           jobs                       │
├─────────────────────────────────────┤
│ id (PK)                             │
│ queue (monitor-checks/notifications)│
│ payload (JSON)                      │
│ attempts                            │
│ reserved_at                         │
│ available_at                        │
│ created_at                          │
└─────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Enter credentials
     │
┌────▼──────────┐
│  LoginView    │
│  (Vue)        │
└────┬──────────┘
     │ 2. POST /api/auth/login
     │    { email, password }
     │
┌────▼────────────────┐
│  AuthController     │
│  @login             │
├─────────────────────┤
│ 3. Validate creds   │
│ 4. Generate JWT     │
└────┬────────────────┘
     │
     │ 5. Return token + user data
     │    { success, token, user }
     │
┌────▼──────────┐
│  Auth Store   │
│  (Pinia)      │
├───────────────┤
│ 6. Save token │
│    localStorage│
│ 7. Save user  │
└────┬──────────┘
     │
     │ 8. Redirect to dashboard
     │
┌────▼─────────────┐
│  DashboardView   │
│                  │
│  Authenticated!  │
└──────────────────┘

Subsequent Requests:
┌─────────────────┐
│  Any API Call   │
└────┬────────────┘
     │ Authorization: Bearer {token}
     │
┌────▼──────────────┐
│  Axios Interceptor│
├───────────────────┤
│ Add token header  │
└────┬──────────────┘
     │
┌────▼─────────────┐
│  Laravel API     │
│  JWT Middleware  │
├──────────────────┤
│ Verify token     │
│ Decode user data │
└──────────────────┘
```

---

## 📈 Metrics Aggregation Flow

```
┌────────────────┐
│  Cron Schedule │
└────────┬───────┘
         │
         │ Every minute
         │
┌────────▼──────────────────────┐
│  AggregateMonitorMetrics      │
│  Command                      │
├───────────────────────────────┤
│ 1. Get all enabled monitors   │
└────────┬──────────────────────┘
         │
         │ For each monitor...
         │
┌────────▼──────────────────────┐
│  Calculate metrics for period │
│  (minute/hour/day)            │
├───────────────────────────────┤
│ SELECT                        │
│   COUNT(*) as total_checks,   │
│   SUM(CASE status='up'...)    │
│      as successful,           │
│   AVG(response_time),         │
│   MIN(response_time),         │
│   MAX(response_time)          │
│ FROM monitor_checks           │
│ WHERE monitor_id = ?          │
│   AND checked_at BETWEEN      │
│       period_start AND end    │
└────────┬──────────────────────┘
         │
         │ Save aggregated data
         │
┌────────▼─────────────────────────┐
│  INSERT INTO                     │
│  monitor_metric_aggregated       │
│                                  │
│  - period = 'minute'             │
│  - period_start = '2024-01-05    │
│                    14:23:00'     │
│  - total_checks = 60             │
│  - successful_checks = 58        │
│  - avg_response_time = 125.5     │
│  - uptime_percentage = 96.67     │
└──────────────────────────────────┘

Hourly & Daily:
  Same process, different period ranges
  - Hourly: Aggregate last hour (60 minutes)
  - Daily: Aggregate last day (24 hours)

Cleanup:
┌────────────────────────┐
│  CleanupOldMonitorData │
│  Command (Daily 2 AM)  │
├────────────────────────┤
│ DELETE FROM            │
│ monitor_checks         │
│ WHERE checked_at <     │
│   NOW() - INTERVAL     │
│   '30 days'            │
└────────────────────────┘
```

---

**Reference: See [RESEARCH_AND_DEVELOPMENT.md](RESEARCH_AND_DEVELOPMENT.md) for detailed explanations.**
